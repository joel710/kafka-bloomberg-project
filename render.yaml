services:
  - type: web
    name: kafka-bloomberg-sentinel
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PORT
        value: 8000
      - key: GOOGLE_API_KEY
        sync: false # À configurer dans le dashboard Render
      - key: MONGO_URI
        sync: false # À configurer dans le dashboard Render
      - key: KAFKA_CA_PEM
        sync: false
      - key: KAFKA_SERVICE_CERT
        sync: false
      - key: KAFKA_SERVICE_KEY
        sync: false
